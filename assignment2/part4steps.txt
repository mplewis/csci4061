BACKUP STEPS (for 'mydir'):
1. Check if mydir.bak exists
       If it does, move mydir.bak to mydir.bak.Feb-13-2013-16-59-08
2. Create mydir.bak
3. For each thing in 'mydir':
       If thing is a file, copy the file from 'mydir/file' to 'mydir.bak/file'
       If thing is a symlink:
           Read symlink source and destination
           Create a symlink in 'mydir.bak' from 'mydir.bak/src' to 'mydir.bak/dst'
       If thing is a folder (example: named 'nestdir'):
           Create the folder in 'mydir.bak' ('mydir.bak/nestdir')
           Descend into it and continue backing up

WHAT'S DONE:
    datetime->buf function (time_to_buf)
    change directories (change_dir)
    create directories (mkdir() built-in)
    create symbolic links (create_symlink)
    get symlink destination (get_symlink_dest)
    rename file/folder (rename() built-in)